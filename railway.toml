[build]
buildCommand = "cd backend && npm install && npx prisma generate && npm run build"

[deploy]
startCommand = "cd backend && npx prisma migrate deploy && npx ts-node prisma/seed.ts && npx ts-node prisma/migrate-local-data.ts && node dist/app.js"
